<!DOCTYPE html>
<html>
   <head>
      <title>Sites</title>
   </head>
   
   <body>
      <b>SITES</b>
      <div>
      {{#sites}}
         <div class=post>
            <a class=topic href="{{url}}">{{name}}</a>
         </div>

         {{/sites}}
      </div>
   </body>
</html>